import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

type Props = {}

const InteriorInformation = (props: Props) => {
  return (
    <View>
      <Text>InteriorInformation</Text>
    </View>
  )
}

export default InteriorInformation

const styles = StyleSheet.create({})